# 生産指示書PDF → ダッシュボード取込用JSON 変換プロンプト

生成AI（Claude / ChatGPT など）に**指示書PDFを添付**して、以下をそのまま貼り付けてください。
出力されたJSONを、ダッシュボードの「入出力・バックアップ」→「貼り付けたJSONを取り込む」に貼ります。

---

## プロンプト（ここから）

添付の生産指示書PDFを読み取り、下記スキーマのJSONだけを出力してください。
説明文・前置き・コードフェンス外の文章は一切不要です。JSONのみを返してください。

### 出力スキーマ

```json
{
  "workOrders": [
    {
      "no": "指示書番号（例: MU26082501）",
      "title": "指示書のタイトル（例: ☆生産指示【差替】）",
      "client": "お取引先名（「様」は除く）",
      "salesRep": "営業担当者名",
      "issuedDate": "発行日 YYYY-MM-DD",
      "dueDate": "完了希望日 YYYY-MM-DD",
      "shipMethod": "出荷方法・時間指定・納品書同梱などをまとめた文字列",
      "paymentTerms": "入金条件",
      "notes": ["特記事項を1件1要素の配列で。修正点メモも含める"]
    }
  ],
  "lots": [
    {
      "id": "案件名の値（例: N081-26071603）。無ければ 指示書No-01 の連番",
      "workOrderNo": "上の no と同じ値",
      "arrivalMonth": "YYYY-MM",
      "status": "入荷予定 / 未着手 / 生産中 / 生産完了 のいずれか",
      "modelName": "品名（例: LIFEBOOK U9311）",
      "supplier": "",
      "receivedQty": 0,
      "rpcEstimateQty": 明細の数量（整数）,
      "rpcConfirmedQty": 0,
      "productionStartDate": "",
      "expectedCompletionDate": "YYYY-MM-DD",
      "project": "",
      "allocatedQty": 0,
      "remarks": "商品詳細・付属品・ストレージ・ソフト・ステッカー・OS・ログイン方法・清掃/検査を ｜ 区切りで連結"
    }
  ]
}
```

### 変換ルール

1. **明細1行 = lots 1件**。数量が空欄の行（No.5以降など）は出力しない。
2. `rpcEstimateQty` に明細の**数量**を入れる。`receivedQty` と `rpcConfirmedQty` は
   指示書には無いので必ず `0`。値を推測して埋めないこと。
3. `expectedCompletionDate` は全ロット共通で**完了希望日**を入れる。
4. `arrivalMonth` は**案件名の数字部分**から取る。
   `N081-26071603` → `26`=2026年 `07`=7月 → `"2026-07"`。
   このパターンに当てはまらない場合は `""`（空文字）にする。**当て推量はしない。**
5. `status` は指示書が出た段階なら `"未着手"`。PDFに進捗の記載があればそれに従う。
6. `project` は納品先の大学・案件が明記されていればその名前。
   「大学在庫生産」のような在庫向けなら、その取引先名をそのまま入れる。
7. `supplier`（調達元・仕入先）は指示書には通常無いので `""`。
   お取引先名を代わりに入れないこと。
8. `remarks` には明細行の付随情報を全部入れる。捨てない。
   例: `Core i5 1145G7/RAM 16GB/... ｜ 付属品: AC、ノートンPPOEM ｜ ストレージ: 新品SSD500GB×197 ｜ ソフト: ノートン360デラックス(PPOEM版)×197 ｜ ステッカー: 有 ｜ OS: 11P64 MAR ｜ ログイン方法: ようこそスタート ｜ 清掃/検査: RPC Academy`
9. `notes` には指示書全体の特記事項・修正点メモ・伝達事項を1件ずつ入れる。
10. 日付は必ず `YYYY-MM-DD`。和暦・曜日・スラッシュ区切りは変換する。
11. 数量はカンマを除いた**整数**。文字列にしない。
12. **PDFに書かれていない値を創作しない。** 不明な文字列は `""`、不明な数値は `0`。
13. 最後に、`lots` の `rpcEstimateQty` の合計が、PDFの合計台数と一致するか検算する。
    一致しない場合は JSON の後に `<!-- 検算NG: 合計 X vs PDF Y -->` と付記する。

## プロンプト（ここまで）

---

## 取り込み手順

1. 上のプロンプト＋PDFをAIに投げる
2. 出力されたJSONをコピー
3. ダッシュボード右上「入出力・バックアップ」を開く
4. 取り込み方法＝**追加・更新（既存データは残す）** を選択（既定）
5. テキストエリアに貼り付け →「貼り付けたJSONを取り込む」

同じ指示書を取り込み直すと、**ロットIDが一致する行は上書き更新**されます（重複しません）。
`全置換` は既存データを全部捨てるので、復元のとき以外は使わないでください。

## 取り込み後にやること

指示書PDFには入荷台数・歩留まりの情報が無いため、以下は手入力になります。

- `入荷台数` … 歩留まり率の分母。ここが0だと歩留まりが計算されません
- `生産開始予定日` … 未入力ならガントの棒は入荷月の初日から始まります
- `割当プロジェクト` / `割当台数` … 大学PJに引き当てるとき
